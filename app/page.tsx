import { Button } from "@/components/ui/button";

const Home = () => {
  return (
    <div className="text-4xl h-screen flex flex-col justify-center items-center font-mono font-medium">
      Sahil Kumar Dev <Button className="mt-2">Click to connect.</Button>
    </div>
  );
};

export default Home;
